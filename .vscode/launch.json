{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug zone_gauche",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/vues/zone_gauche.py",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}